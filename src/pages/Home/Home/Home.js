import React, { useContext } from 'react';
import './Home.css'
import { AuthContext } from '../../../contexts/AuthProvider';

const Home = () => {

    return (
        <div>

        </div>
    );
};

export default Home;